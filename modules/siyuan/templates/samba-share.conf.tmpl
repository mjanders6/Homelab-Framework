# DEPRECATED — not used.
#
# The Samba share block is now written inline by modules/siyuan/configure.sh,
# matching the heredoc+grep-guard pattern used by
# scripts/configure/configure_samba_shares.sh, rather than via a rendered
# conf.d include file (the framework doesn't use conf.d includes anywhere
# else, so this template diverged from convention). Kept only so the file
# history shows what changed; safe to delete.
