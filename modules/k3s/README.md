# k3s module

The `k3s` module provides K3s cluster deployment support for diskless Pi workers.

## Capabilities

- install
- configure
- verify
- status
- remove

## Dependencies

- `docker`
- `network`

## Usage

```bash
make install-k3s
make verify-k3s
make status-k3s
```
