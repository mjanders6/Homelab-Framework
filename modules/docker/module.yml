name: docker
description: Container runtime support for Kubernetes and other services.
version: 1.0.0
category: infrastructure
capabilities:
  - install
  - configure
  - verify
  - status
  - remove
dependencies:
  - filesystem
