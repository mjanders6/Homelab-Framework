# network module

The `network` module provides validation helpers and health checks for the homelab network stack.

## Capabilities

- install
- verify
- status
- remove

## Usage

```bash
make install-network
make verify-network
make status-network
```
